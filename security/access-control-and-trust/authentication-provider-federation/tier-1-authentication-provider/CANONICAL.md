# Canonical Location

The production code for this solution lives at:

<https://code.europa.eu/simpl/simpl-open/development/iaa/keycloak-authenticator>

This folder in the catalogue is a structural prototype. The code is illustrative, not authoritative.

> Local folder is named `tier-1-authentication-provider` (capability-map name); upstream gitlab repo is `iaa/keycloak-authenticator`. Distinct from `iaa/keycloak-authenticator-plugin` (the custom Keycloak SPI), which lives at [`identity-provider-federation/keycloak-authenticator-plugin/`](../../identity-provider-federation/keycloak-authenticator-plugin/README.md).
